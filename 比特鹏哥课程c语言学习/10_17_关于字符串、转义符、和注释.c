#include <stdio.h>
int main() {
	printf("c语言中有字符串这中数据类型吗？-没有。\n");
	printf("每一们语言有每一门语言的数据类型和数据结构。\n");
	printf("目前来看，c语言的数据类型有char\int\float\double\long\short\long long\这七种，数据结构目前只看到数组一种。");
	printf("c语言里有指针，内存管理，也有函数，但是不知道有没有类、属性、对像、方法，但是一定有文件操作。");
	printf("双引号括起来的是字符串，单引号括起来的是单个字符。") 
	return 0;
}
