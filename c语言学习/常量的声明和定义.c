#include <stdio.h>

int main() {
    const int shui_fei_dian=100;//这是一个常量，用const关键字来声明。
    /*
    声明常量的格式：const 数据类型 常量名 = 值;
    注意，定义常量时，值是一定要赋上的，不能先声明后再另起一行给常量赋值。否则，就会报错。
    约定俗成的是，定义常量时，名字要全大写。方便后续使用。
    */
    return ;
}